# 🧶 手工编绳长度计算器

## 项目简介

这是一个为手工编绳爱好者设计的在线计算工具，用于精准计算不同编绳方式所需的线长。

## ✨ 主要功能

### 支持的编绳类型（11种）
- **2倍类**：二股辫、四股辫、六股辫、八股辫、十股辫、双锁结
- **9倍类**：蛇结
- **10倍类**：金刚结、单向平结、双向平结、玉米结

### 支持的线型
- 72号玉线（基准，调整系数1.0）
- A线（较粗，调整系数1.1）
- B线（略粗，调整系数1.05）
- 0.8mm（较细，调整系数0.9）
- 1mm（标准，调整系数1.0）
- 1.2mm（较粗，调整系数1.15）

### 特殊功能
- ✅ 自动计算握持余量（15-20cm）
- ✅ 支持包心蛇结额外增加量（+20%）
- ✅ 自动调整线型倍数
- ✅ 精确到小数点后一位

---

## 📐 计算公式

```
基础线长 = 手圈长度 × 编绳类型倍数 × 线型调整系数

如果是包心蛇结：
  基础线长 = 基础线长 × 1.2

最终线长 = 基础线长 + 17cm（握持余量中值）

每条线长 = 最终线长 ÷ 2
```

### 计算示例

**场景**：制作20cm手圈，使用金刚结编法，线型为72号玉线

1. 倍数查询：金刚结 = 10倍
2. 线型系数：72号玉线 = 1.0
3. 基础计算：20 × 10 × 1.0 = 200cm
4. 加握持余量：200 + 17 = 217cm
5. 每条线：217 ÷ 2 = **108.5cm**

**结果**：需要剪2条各 **108.5cm** 的72号玉线

---

## 🚀 部署说明

### 本地测试
```bash
# 启动后端（8000端口）
cd backend
python -m uvicorn main:app --host 127.0.0.1 --port 8000

# 启动前端（5500端口）
cd frontend
python -m http.server 5500 --bind 127.0.0.1

# 打开浏览器访问
http://127.0.0.1:5500
```

### 部署到 Render

#### 部署后端（Web Service）
1. 连接 GitHub repository
2. Root Directory: `backend`
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `python -m uvicorn main:app --host 0.0.0.0 --port $PORT`

#### 部署前端（Static Site）
1. 连接 GitHub repository
2. Root Directory: `frontend`
3. Build Command: `echo "No build step"`
4. Publish Directory: `.`

#### 配置前端 API 地址
编辑 `frontend/script.js`，将 `API_URL` 改为你的 Render 后端 URL：
```javascript
const API_URL = "https://your-backend-service.onrender.com";
```

---

## 📁 项目结构

```
simple-fullstack-render-demo/
├── backend/
│   ├── main.py              # FastAPI 后端应用
│   └── requirements.txt      # Python 依赖
├── frontend/
│   ├── index.html           # 计算器页面
│   ├── script.js            # 交互逻辑
│   └── style.css            # 样式设计
├── 编绳计算器说明.md         # 本文件
└── README.md
```

---

## 🛠 技术栈

- **前端**：HTML5 + CSS3 + Vanilla JavaScript
- **后端**：Python + FastAPI + Pydantic
- **部署**：Render（Web Service + Static Site）
- **数据交互**：RESTful API + JSON

---

## 📝 API 端点

### 计算编绳长度
```
POST /api/calculate

请求体：
{
  "bracelet_length": 20,        # 手圈长度（cm）
  "knot_type": "金刚结",        # 编绳类型
  "thread_type": "72号玉线",    # 线型
  "is_heart_snake": false       # 是否包心蛇结
}

响应体：
{
  "success": true,
  "bracelet_length": 20,
  "knot_type": "金刚结",
  "thread_type": "72号玉线",
  "base_length": 200,           # 不含握持余量
  "final_length": 217,          # 含握持余量
  "per_thread_length": 108.5,   # 每条线长
  "message": "需要剪2条各108.5cm的72号玉线，共217cm"
}
```

### 获取支持的编绳类型
```
GET /api/knot-types
```

### 获取支持的线型
```
GET /api/thread-types
```

---

## 📚 使用说明

1. **输入手圈长度**：填入你要制作的手圈或项链长度（厘米）
2. **选择编绳类型**：从11种编绳方式中选择
3. **选择线型**：根据你使用的线的类型选择
4. **勾选包心（可选）**：如果是包心蛇结，勾选此选项
5. **点击计算**：获取精确的剪线长度

计算结果包括：
- ✂️ 所需总线长
- 📏 每条线的长度（通常是2条线）
- 💡 具体建议信息

---

## ⚙️ 修改和扩展

### 添加新的编绳类型
编辑 `backend/main.py`，在 `KNOT_MULTIPLIERS` 字典中添加：
```python
KNOT_MULTIPLIERS = {
    # ... 现有类型 ...
    "新编绳名": 倍数,
}
```

### 添加新的线型
在 `THREAD_ADJUSTMENTS` 字典中添加：
```python
THREAD_ADJUSTMENTS = {
    # ... 现有线型 ...
    "线型名": 调整系数,
}
```

### 修改握持余量
编辑 `GRIP_LENGTH` 的值（单位：厘米）：
```python
GRIP_LENGTH = 17  # 改为你需要的长度
```

---

## 🔧 常见问题

**Q：为什么我的计算结果和预期不符？**
A：请检查：
1. 手圈长度单位是否为厘米
2. 编绳类型是否选择正确
3. 线型是否与你实际使用的线匹配

**Q：为什么有17cm的握持余量？**
A：这是根据手工编绳的经验总结，方便拿着线编绳。如需调整，可在后端代码中修改 `GRIP_LENGTH` 变量。

**Q：线型调整系数是如何计算的？**
A：系数是相对于72号玉线的比例：
- 粗的线需要增加系数（如A线 1.1，表示增加10%）
- 细的线需要减少系数（如0.8mm 0.9，表示减少10%）

---

## 📞 反馈与建议

如有改进建议或发现问题，欢迎：
- 提交 Issue
- 创建 Pull Request
- 联系项目维护者

---

**祝编绳快乐！** 🎀

最后更新：2026年8月15日
