# 🧶 手工編繩長度計算器

## 專案簡介

這是一個為手工編繩愛好者設計的線上計算工具，用於精準計算不同編繩方式所需的線長。

## ✨ 主要功能

### 支持的編繩類型（11種）
- **2倍類**：二股辮、四股辮、六股辮、八股辮、十股辮、雙鎖結
- **9倍類**：蛇結
- **10倍類**：金剛結、單向平結、雙向平結、玉米結

### 支持的線型
- 72號玉線（基準，調整係數1.0）
- A線（較粗，調整係數1.1）
- B線（略粗，調整係數1.05）
- 0.8mm（較細，調整係數0.9）
- 1mm（標準，調整係數1.0）
- 1.2mm（較粗，調整係數1.15）

### 特殊功能
- ✅ 自動計算握持餘量（15-20cm）
- ✅ 支持包心蛇結額外增加量（+20%）
- ✅ 自動調整線型倍數
- ✅ 精確到小數點後一位

---

## 📐 計算公式

```
基礎線長 = 手圈長度 × 編繩類型倍數 × 線型調整係數

如果是包心蛇結：
  基礎線長 = 基礎線長 × 1.2

最終線長 = 基礎線長 + 17cm（握持餘量中值）

每條線長 = 最終線長 ÷ 2
```

### 計算示例

**場景**：製作20cm手圈，使用金剛結編法，線型為72號玉線

1. 倍數查詢：金剛結 = 10倍
2. 線型係數：72號玉線 = 1.0
3. 基礎計算：20 × 10 × 1.0 = 200cm
4. 加握持餘量：200 + 17 = 217cm
5. 每條線：217 ÷ 2 = **108.5cm**

**結果**：需要剪2條各 **108.5cm** 的72號玉線

---

## 🚀 部署說明

### 本地測試
```bash
# 啟動後端（8000連接埠）
cd backend
python -m uvicorn main:app --host 127.0.0.1 --port 8000

# 啟動前端（5500連接埠）
cd frontend
python -m http.server 5500 --bind 127.0.0.1

# 打開瀏覽器訪問
http://127.0.0.1:5500
```

### 部署到 Render

#### 部署後端（Web Service）
1. 連接 GitHub repository
2. Root Directory: `backend`
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `python -m uvicorn main:app --host 0.0.0.0 --port $PORT`

#### 部署前端（Static Site）
1. 連接 GitHub repository
2. Root Directory: `frontend`
3. Build Command: `echo "No build step"`
4. Publish Directory: `.`

#### 配置前端 API 地址
編輯 `frontend/script.js`，將 `API_URL` 改為你的 Render 後端 URL：
```javascript
const API_URL = "https://your-backend-service.onrender.com";
```

---

## 📁 專案結構

```
simple-fullstack-render-demo/
├── backend/
│   ├── main.py              # FastAPI 後端應用
│   └── requirements.txt      # Python 依賴
├── frontend/
│   ├── index.html           # 計算器頁面
│   ├── script.js            # 交互邏輯
│   └── style.css            # 樣式設計
├── 編繩計算器說明.md         # 本文件
└── README.md
```

---

## 🛠 技術棧

- **前端**：HTML5 + CSS3 + Vanilla JavaScript
- **後端**：Python + FastAPI + Pydantic
- **部署**：Render（Web Service + Static Site）
- **資料交互**：RESTful API + JSON

---

## 📝 API 端點

### 計算編繩長度
```
POST /api/calculate

請求體：
{
  "bracelet_length": 20,        # 手圈長度（cm）
  "knot_type": "金剛結",        # 編繩類型
  "thread_type": "72號玉線",    # 線型
  "is_heart_snake": false       # 是否包心蛇結
}

響應體：
{
  "success": true,
  "bracelet_length": 20,
  "knot_type": "金剛結",
  "thread_type": "72號玉線",
  "base_length": 200,           # 不含握持餘量
  "final_length": 217,          # 含握持餘量
  "per_thread_length": 108.5,   # 每條線長
  "message": "需要剪2條各108.5cm的72號玉線，共217cm"
}
```

### 獲取支持的編繩類型
```
GET /api/knot-types
```

### 獲取支持的線型
```
GET /api/thread-types
```

---

## 📚 使用說明

1. **輸入手圈長度**：填入你要製作的手圈或項鍊長度（公分）
2. **選擇編繩類型**：從11種編繩方式中選擇
3. **選擇線型**：根據你使用的線的類型選擇
4. **勾選包心（可選）**：如果是包心蛇結，勾選此選項
5. **點擊計算**：獲取精確的剪線長度

計算結果包括：
- ✂️ 所需總線長
- 📏 每條線的長度（通常是2條線）
- 💡 具體建議資訊

---

## ⚙️ 修改和擴展

### 新增編繩類型
編輯 `backend/main.py`，在 `KNOT_MULTIPLIERS` 字典中新增：
```python
KNOT_MULTIPLIERS = {
    # ... 現有類型 ...
    "新編繩名": 倍數,
}
```

### 新增線型
在 `THREAD_ADJUSTMENTS` 字典中新增：
```python
THREAD_ADJUSTMENTS = {
    # ... 現有線型 ...
    "線型名": 調整係數,
}
```

### 修改握持餘量
編輯 `GRIP_LENGTH` 的值（單位：公分）：
```python
GRIP_LENGTH = 17  # 改為你需要的長度
```

---

## 🔧 常見問題

**Q：為什麼我的計算結果和預期不符？**
A：請檢查：
1. 手圈長度單位是否為公分
2. 編繩類型是否選擇正確
3. 線型是否與你實際使用的線相符

**Q：為什麼有17cm的握持餘量？**
A：這是根據手工編繩的經驗總結，方便拿著線編繩。如需調整，可在後端代碼中修改 `GRIP_LENGTH` 變數。

**Q：線型調整係數是如何計算的？**
A：係數是相對於72號玉線的比例：
- 粗的線需要增加係數（如A線 1.1，表示增加10%）
- 細的線需要減少係數（如0.8mm 0.9，表示減少10%）

---

## 📞 反饋與建議

如有改進建議或發現問題，歡迎：
- 提交 Issue
- 創建 Pull Request
- 聯絡專案維護者

---

**祝編繩快樂！** 🎀

最後更新：2026年8月15日